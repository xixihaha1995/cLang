#include <stdio.h>
#include <stdlib.h>

#define BUFFSIZE 1024

int main(int argc, char * argv)
{
    int sfd, dfd;
    int rc, wc;
    char buf[BUFFSIZE];
    open();
    open();

    while(1)
    {
        rc = read();
        write();
    }

    close();
    close();

}
